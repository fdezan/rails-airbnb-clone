
<h2>Your portfolio starts here, junior!</h2>
<p>Create and organize your expertises. We will match projects for you.</p>

<%= link_to 'Add a new service', new_my_service_path, class:"btn btn-primary" %>

<hr>


<div class="container padded">
   <div class="row">
     <% @my_services.each do |my_service| %>
     <div class="col-xs-12 col-sm-4">
       <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path background_url(my_service), height: 300, width: 400, crop: :fill %>')">
       <div class="card-category"><%= my_service.category %></div>
        <div class="card-description">
           <h2><%= my_service.description %></h2>
         </div>
         <%= link_to "", my_service_path(my_service), class: "card-link" %>
       </div>
     </div>
     <% end %>
   </div>
 </div>

